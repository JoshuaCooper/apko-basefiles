version: '3.8'
 
services:
  apk-proxy:
    image: nginx:alpine
    container_name: wolfi-apk-proxy
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./cache:/var/cache/nginx
    restart: unless-stopped
